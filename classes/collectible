import pygame

class Collectible(pygame.sprite.Sprite):
    def __init__(self, starting_pos, type):
        super().__init__()
        self.image = pygame.image.load(f"assets/collectibles/{type}.png").convert()
        self.rect = self.image.get_rect()
        self.rect.center = starting_pos